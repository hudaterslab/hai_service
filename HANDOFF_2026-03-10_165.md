# Handoff - 2026-03-10 (192.168.1.165)

## Goal
- Deploy the VMS stack to `192.168.1.165` under `/media/fishduke/06800C3B800C3429/WorkWithCodex/vms-8ch-webrtc`.
- Keep runtime media data on the target disk path.

## What Was Done
- Uploaded the project to:
  - `/media/fishduke/06800C3B800C3429/WorkWithCodex/vms-8ch-webrtc`
- Created:
  - `deploy/.env`
- Started the Docker stack with:
  - `docker compose -f deploy/docker-compose.yml --env-file deploy/.env up -d --build`
- Verified API health:
  - `http://127.0.0.1:8080/healthz` -> `{"ok":true}`
- Re-checked on 2026-03-10:
  - `docker compose ... ps` shows all 6 services up
  - `vms-postgres` is healthy
  - API health still returns `{"ok":true}`

## Compose/Data Notes
- Runtime media/redis data is configured under:
  - `/media/fishduke/06800C3B800C3429/WorkWithCodex/vms-8ch-webrtc/runtime`
- Postgres could not use a bind mount on that disk because the filesystem does not allow the permission changes required by the official Postgres image.
- To fix that, `postgres` was switched back to a Docker named volume:
  - `deploy_vms_pg_data`
- Result:
  - media artifacts remain on the target disk
  - postgres data lives in Docker-managed storage

## Current Remote Status (192.168.1.165)
- Running containers:
  - `vms-api`
  - `vms-event-recorder`
  - `vms-delivery-worker`
  - `vms-mediamtx`
  - `vms-postgres`
  - `vms-redis`
- `vms-postgres` is healthy.
- API health endpoint responds successfully.
- Current compose project resources confirmed:
  - container names use the `vms-*` naming shown above
  - postgres volume is `deploy_vms_pg_data`

## Known Blocker
- DXRT/DXNN host-side inference setup is not complete on `192.168.1.165`.
- `scripts/linux/install_dxrt_host.sh` failed during DXRT build with missing `ONNXLIB_DIRS`.
- `scripts/linux/install_dxnn_host_service.sh` also could not fully start because `dxrt.service` was not present.
- This means host inference at `http://host.docker.internal:18081/infer` is not yet confirmed working.

## What Is Confirmed vs Not Confirmed

Confirmed:

- Compose stack starts successfully with the checked-in deployment files
- API responds on `http://127.0.0.1:8080/healthz`
- Postgres, Redis, MediaMTX, API, recorder, and delivery worker containers are running
- Runtime path from `deploy/.env` points to:
  - `/media/fishduke/06800C3B800C3429/WorkWithCodex/vms-8ch-webrtc/runtime`

Not yet confirmed:

- end-to-end AI inference through DXRT/DXNN host bridge
- event creation from the actual AI inference path
- artifact generation and delivery against real camera streams
- production hardening items such as auth enablement, secret rotation, and retention tuning

## Important Context From Previous Handoff
- No older handoff file is present in this repository snapshot.
- If prior deployment context exists, it was not checked into the current tree.
- Treat the DXRT/DXNN notes in this document as the only preserved context for that unfinished work.

## Suggested Next Steps
1. On `192.168.1.165`, verify whether AI features are required immediately.
2. If AI inference is required, fix DXRT host installation first:
   - inspect DXRT build prerequisites
   - resolve `ONNXLIB_DIRS` requirement
   - confirm `dxrt.service`
   - confirm `dxnn-host-infer.service`
3. Verify recorder event generation end-to-end:
   - check `/events`
   - check `ai_detection_logs`
   - run a one-shot recorder path if needed
4. If only base VMS features are needed for now, keep the current stack as-is and defer DXRT work.

## Useful Commands
```bash
cd /media/fishduke/06800C3B800C3429/WorkWithCodex/vms-8ch-webrtc
sudo docker compose -f deploy/docker-compose.yml --env-file deploy/.env ps
curl http://127.0.0.1:8080/healthz
sudo docker logs vms-api --tail 200
sudo docker logs vms-event-recorder --tail 200
sudo docker logs vms-postgres --tail 100
```
